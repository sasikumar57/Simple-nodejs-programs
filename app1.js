// Import the lodash library
const _ = require('lodash');

// Simple function using lodash
function addNumbers(a, b) {
  return _.add(a, b);
}

// Call the function and log the output
console.log(addNumbers(5, 10));